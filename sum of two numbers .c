#include <stdio.h>

int main() {
    // Write C code here
    int a, b, sum=0;
    printf("enter the two numbers :");
    scanf("%d %d",&a,&b);
    sum =a+b;
    printf("sumof the two numbers are %d",sum);

    return 0;
}
